# Solitario Classico Accessibile - Runtime Dependencies
# Version: 2.3.1 (refactoring-engine branch)
# Python: >=3.8

# ============================================================================
# CORE DEPENDENCIES (Required for runtime)
# ============================================================================

# UI Framework - wxPython for native dialogs and event loop
wxPython==4.1.1

# Screen Reader / TTS Support
accessible-output2==0.16

# Windows API (required for NVDA integration on Windows)
# These are dependencies of accessible-output2
pywin32==304
pywin32-ctypes==0.2.0

# ============================================================================
# TRANSITIVE DEPENDENCIES (Auto-installed by above packages)
# ============================================================================
# These are listed for reference but will be installed automatically:
#
# From accessible-output2:
# - six==1.16.0
# - winpaths==0.2  (Windows only)
# - PyOpenAL==0.7.11a1  (optional audio backend)
#
# From wxPython:
# - pillow (optional, for image support - not used in this project)
# - numpy (optional, for advanced features - not used in this project)

# ============================================================================
# AUDIO SYSTEM (v3.4.0 - re-aggiunta pygame)
# ============================================================================

# Audio System: 5 bus indipendenti con panning stereo nativo (RF-1 .. RF-6)
# Nota: pygame era stato rimosso da requirements.txt (v2.3.1) dopo la migrazione
# wxPython, ma era ancora usato per keyboard constants (KMOD_SHIFT, KMOD_CTRL)
# in gameplay_controller.py e input_handler.py. Re-aggiunto esplicitamente.
pygame>=2.1.2

# ============================================================================
# REMOVED DEPENDENCIES (v2.3.1 - wxPython migration)
# ============================================================================
# pygame==2.1.2              # RE-AGGIUNTO in v3.4.0 (vedi sezione AUDIO SYSTEM sopra)
# pygame-menu==4.3.7         # Migrated to wxPython (Issue #58)
# numpy==1.23.0              # Not used in current codebase
# Pillow==9.2.0              # Not used in current codebase
# playsound==1.3.0           # Using accessible-output2 instead
# gTTS==2.2.4                # Using accessible-output2 instead
# requests==2.28.1           # Not used
# pyperclip==1.8.2           # Not used
# markdown2==2.4.3           # Not used
# lief==0.12.1               # Not used
# libloader==0.21            # Not used

# ============================================================================
# DEV/BUILD TOOLS (Moved to separate files)
# ============================================================================
# See dev-requirements.txt for:
# - flake8, pylint, pycodestyle, pyflakes, isort, mccabe
# - astroid, typing_extensions, tomli, tomlkit
#
# See build-requirements.txt for:
# - cx-Freeze, cx-Logging
# - pyinstaller-hooks-contrib
